type Query {
    _placeholder: String
}

type Mutation {
    updateProvider(id: ID!, input: ProviderInput!): Provider
}

input ProviderInput {
    name: String
    address: String
    email: String
}

type Provider {
    id: ID!
    name: String!
    address: String!
    email: String!
}
